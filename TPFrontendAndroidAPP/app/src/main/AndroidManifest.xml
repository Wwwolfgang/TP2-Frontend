<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tp_frontend_androidapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_INTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="actividadPaciente.DetallePaciente.Detalle"></activity>
        <activity android:name="actividadPaciente.RegistroPaciente.CrearPaciente"
            android:colorMode="hdr"/>
        <activity android:name=".ListaDoctorActivity" />
        <activity android:name=".ListaFichaActivity" />
        <activity android:name=".MenuPrincipalActivity" />
        <activity android:name=".ReservaActivity"/>
        <activity android:name=".EditarAgregarReservaActivity"/>
        <activity android:name=".CrearFichaActivity" />
        <activity android:name=".CrearReservaActivity" />
        <activity android:name="actividadPaciente.PacientesActivity"
            android:colorMode="hdr"/>
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>